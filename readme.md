# Regular expressions to finite automata

## Requirements
### Mac
Instal graphviz `brew install graphviz`

### Ubuntu (not tested)
Instal graphviz `sudo apt install graphviz`


## Usage example
`"(aa)*(b + aba)(aa)*" -o x.pdf`